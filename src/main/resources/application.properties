spring.application.name=ecommerce-amazon-backend

# BANCO DE DADOS
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d835rs3rjlhs7384q3n0-a.oregon-postgres.render.com:5432/db_ecommerc_amazon}
spring.datasource.username=${DB_USERNAME:db_ecommerc_amazon_user}
spring.datasource.password=${DB_PASSWORD:uZMO6bduURriMkE8guefLdmiKbrpJult}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
api.security.token.secret=${JWT_SECRET:minha-chave-super-secreta-do-projeto-poo-2026-unicap}

# E-MAIL (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
